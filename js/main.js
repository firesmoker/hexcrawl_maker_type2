import { PPI, allTerrains, allAddons, iconSvgs } from './config.js';

console.log("Main.js loaded successfully.");
console.log("Config loaded:", { PPI, allTerrains, allAddons, iconCount: Object.keys(iconSvgs).length });
